import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'big3MobileAPP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
